# WearNest Backend

Express + MongoDB (Mongoose) backend.

## Quick start
1) Copy `.env.example` to `.env` and fill required values.
2) Start server:

```bash
npm run dev
```

## Endpoints
Base path: `/api`
- `GET /api/health`

Authentication and other routes will be available once implemented.

